


            // Get DataBase Collections FIRESTORE
              // const CharacterList = await getDocs(collection(db, 'Characters'));
              //const WeaponList = await getDocs(collection(db, 'Weapons'));
              //const ArtifactList = await getDocs(collection(db, 'Artifacts'));
              //const VariablesList = await getDocs(collection(db, 'Variables'));
            //const TypesList = await getDoc(doc(db, "Types", "Type"));

            // Get All Characters
              // CharacterList.docs.map((Character) =>{        
              //   setLoaded(true)
              //   return window["DataChara"][Character.id] = Character.data(); // Save CharacterList to window.DataChara
              // });

            // Weapon List
              //WeaponList.docs.map((WeaponType) => { return window[WeaponType.id] = WeaponType.data() }); // Save Weapon Type (Sword, Bow etc..) With Content

            // All Artifacts
              //ArtifactList.docs.map((ArtifactSet) => { return window["Arte"][ArtifactSet.id] = ArtifactSet.data(); }); // Save All Artifacts

              // All Variables
            //VariablesList.docs.map((Variable) =>{ return window[Variable.id] = Variable.data()[0] }); // Save All Variables in Window[Variable]

            // All Types (Dendro etc..)
            //window["Type"] = TypesList.data()[0] ;  